package json

import play.api.libs.json.Json

class JsonParser {

  /*def parse(json: Json): Unit = {
    json match {
      case Numeric(num) => num
      case BooleanType(bool) => bool
      case StringType(str) => str
      case Object(objectVal) => (objectVal._1, objectVal._2.map(l => parse(l)))
      case ListObject(list) => list.map(l => parse(l))
    }
  }


  val jsonSample = Object("person",
                      List(Numeric(("age", 1)), StringType("name","Milind")))


  parse(jsonSample)
*/
}
